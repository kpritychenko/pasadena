import * as React from 'react';

import ASD from 'app/views';
import './styles.less';

export default class App extends React.Component<> {
    render() {
        return (
            <div>
                <ASD />
            </div>
        )
    }
}